//Useful comments

// function isTouching(target1, target2) {
this.touches = function (target) {
    // right = this.x + (this.width / 2);
    // left = this.x - (this.width / 2);
    // down = this.y + (this.height / 2);
    // up = this.y - (this.height / 2);
    // allSides = [right, left, up, down];
    // if (touchSide === right) {
    //   if ((target.x - this.x) < (target.width + this.width) / 2) {
    //     return true;
    //   }
    // }
    // if (touchSide === left) {
    //   if (this.x - (target.x) < (target.width + this.width) / 2) {
    //     return true;
    //   }
    // }
    // if (touchSide === up) {
    //   if ((target.y - this.y) < (target.height + this.height) / 2) {
    //     return true;
    //   }
    // }
    // if (touchSide === down) {
    //   if ((this.y - target.y) < (target.height + this.height) / 2) {
    //     return true;
    //   }
    // }

    // if (touchSide === allSides) {
    //   if (((target.x - this.x) < (target.width + this.width) / 2)
    //     && (this.x - (target.x) < (target.width + this.width) / 2)
    //     && ((target.y - this.y) < (target.height + this.height) / 2)
    //     && ((this.y - target.y) < (target.height + this.height) / 2)) {
    //     return true;
    //   }
    // }
}


// if (turbo.shouldMove) {
//     setVelocity(turbo, turbo.speed, 0);
//   }
//   else {
//     setVelocity(turbo, 0, 0);
//   }

//   if (gallardo.shouldMove) {
//     setVelocity(gallardo, gallardo.speed, 0);
//   }
//   else {
//     setVelocity(gallardo, 0, 0);
//   }

//   if (viper.shouldMove) {
//     setVelocity(viper, viper.speed, 0);
//   }
//   else {
//     setVelocity(viper, 0, 0);
//   }

//   if (enzo.shouldMove) {
//     setVelocity(enzo, enzo.speed, 0);
//   }
//   else {
//     setVelocity(enzo, 0, 0);
//   }

